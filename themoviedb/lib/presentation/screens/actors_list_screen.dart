import 'package:flutter/material.dart';

class ActorsListScreen extends StatelessWidget {
  const ActorsListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Actors list sreeen'),
      ),
    );
  }
}
